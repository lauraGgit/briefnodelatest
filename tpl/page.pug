doctype html
html(lang="en")
    include includes/head.pug
    body
      script(src='sdk-load.js')
      script(src='main.js')
      #exampleModal.modal.fade(tabindex="-1", role="dialog", aria-labelledby="myModalLabel", aria-hidden="true")
          .modal-dialog
            .modal-content
              .modal-header
                button.close(type="button", data-dismiss="modal", aria-hidden="true") &times;
                h4 A Sample
                 img.logo-modal(src='images/brief-logo.svg' title="brief")
              .modal-body
                p Every morning you'll wake up to a new Brief of the activity in the past day. You can also choose if you'd like to see previously read messages that might have gotten lost in the fray of your busy life.
                img.exmpl(src='images/brief-hp.jpg')
      #content
          #title
            //h1 Brief
            img.logo(src='images/brief-logo.svg' title="Brief")
            h6 Your Daily Facebook Email
            a.btn.btn-primary.btn-xs(class="faq-link", href="/faq") FAQ
          #welcome
            h5 What is Brief?
            p Get a daily summary of all of your important Facebook
              br
              |notifications without have to go onto Facebook.
            button.btn.btn-primary(data-toggle="modal", data-target="#exampleModal") Check out an example email

            h5(style="margin-top: 2.25em;") Get Your Brief
            p Sign up for Brief by connecting your Facebook
              br
              |account. Then we will create your account.
            button#auth.btn.btn-primary.btn-lg(style='display:none') Connect your Facebook account
      #loading(style='width: 750px; margin: 10px;display:none;')
          h2 Look at you, you're already logged in to Facebook.
          h3 Hang out a sec while we pull up your settings
          img(id='load-gif', class='load-gif', src='images/loading.gif')
      #settings(style='width: 750px; margin: 10px;display:none;')
          h2 Settings
          form#setForm
            label(for='email') Email
            input#email.form-control(type='text' name='email' data-validation="email" data-validation-help="Please enter a valid email" disabled)
            small You may only use the email address you have on file with Facebook.
            h4 Brief Options
            label(for='inRead') Only show new notifications
              br
              input#read(type='radio', name ='inRead', value='0')
              |Yes
              input#unread(type='radio', name ='inRead', value='1')
              |No
            br
            label(for='markRead') Mark My Notifications as Read
              br
              input#read(type='radio', name ='markRead', value='1')
              |Yes
              input#unread(type='radio', name ='markRead', value='0')
              |No
            br
            label(for='dayOld') Include notifications older than a day
              br
              input#dayOld(type='radio', name ='dayOld', value='1')
              |Yes
              input#noDayOld(type='radio', name ='dayOld', value='0')
              |No
            br
            button#save-changes.btn.btn-primary(type=submit) Update Your Preferences
            button#unsubscribe.btn.btn-primary Unsubscribe
          br
          #notification.alert(style="display:none;")
      include includes/footer.pug
